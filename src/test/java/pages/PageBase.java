package pages;

import helpers.UrlManager;
import helpers.ValidationMessageManager;

public class PageBase {

    UrlManager urlManager = new UrlManager();

    ValidationMessageManager validationMessageHelper = new ValidationMessageManager();
}
